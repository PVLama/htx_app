import 'package:flutter/material.dart';

class DetailHotelPage extends StatelessWidget {
  const DetailHotelPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
